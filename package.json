{
  "name": "tasks-tracker",
  "version": "0.1.0",
  "private": true,
  "description": "Very simple tasks tracker V1 (HTML + JS only)",
  "scripts": {
    "start": "http-server . -p 8080 -c-1"
  },
  "dependencies": {},
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}

